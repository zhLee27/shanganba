## 2.4.1（2026-09-20）

- 优化：去掉行背景的绿色底——未选中的行改成中性浅灰，只有选中的那行才带所属题型的颜色
- 优化：题型行上下留白加大，不再显得挤

## 2.4.0（2026-09-20）

- 优化：去掉点击时那层半透明水波纹，改成只有颜色变化（全局生效）
- 修复：放弃计时 / 切页面回来后，「本次刷什么」的展开状态与选中题型保持不变
- 修复：刷题列表选中配色统一——选中行、名称、圆点、播放按钮都用该题型自己的颜色，切科目时自动选中第一个大题型，不会再出现整页不高亮或颜色串台
- 优化：行测 / 申论切换改带动画过渡（选中胶囊与文字颜色平滑渐变）

## 2.3.0（2026-09-20）

- 优化：「本次刷什么」行样式改为方案 B——每行浅底卡片，左侧只留细线箭头（去掉圆底），右侧计时按钮缩到 28dp

## 2.2.0（2026-09-20）

- 移除：「本次刷什么」里的排序模式、编辑与删除按钮，题型按默认顺序展示（新增/改名/删除统一在「任务模板」里管理）

## 2.1.0（2026-09-20）

- 新增：每日计划提醒支持按星期单独设置时间（点某天的时间即可单独调整，灰色表示沿用统一时间）
- 新增：首页「添加今天的任务」支持关联大题型 / 小题型，并可设数量与单位（题 / 篇 / 组 / 道 / 分钟）

## 2.0.0（2026-09-20）

- 优化：展开小三角换成带底色的圆形箭头图标，更大更明显（有小题型时高亮）
- 优化：计时按钮缩小到 32dp（小题型 28dp），三角形按视觉重心居中
- 修复：从计时页「放弃本次」后回到进入计时前的那一页（行测 / 申论 tab 不再跳回行测）
- 优化：行测 / 申论切换栏换成胶囊分段样式，和 App 整体风格一致
- 优化：分析页内容真正分开——行测看雷达、行测估分、趋势、薄弱项；申论只看申论估分与各题型得分率
- 修复：「我的」里每日提醒的星期列表可以横向滑动，窄屏不再被挤住

## 1.9.0（2026-09-20）

- 更新：行测题型与分值按 2027 最新分布重做（政治理论 15×0.7、常识 15×0.8、言语 25×0.9、数量 15×0.9、图形 5×0.9、定义 10×0.9、类比 10×0.8、逻辑 10×1.0、资料 20×0.9），小题型同步细化（含常识六大类、数量六大题型、判断四个小题型、资料五类）
- 优化：「本次刷什么」按行测 / 申论分两个标签页切换
- 优化：计时按钮改成更大的实心圆 + 居中三角形，一眼能看见
- 优化：题型行左侧留白收紧，文字更靠左
- 修复：老数据的题型记录自动归到新结构（按前缀归入大题型）

## 1.8.0（2026-09-19）

- 新增：头像、昵称、签名改为跟着账号保存，退出再登录会自动带回来
- 新增：每日计划提醒改成闹钟式——周一到周日可多选，再配具体时间
- 新增：未登录时只能浏览页面，点任何功能都会提示需要登录并询问是否跳转登录页（「我的」页仍可进入去登录）
- 新增：登录页实时校验——手机号格式、密码长度与字符组成边输边提示

## 1.7.1（2026-09-19）

- 优化：「本次刷什么」改为长按任意题型进入排序模式（或点右上角「排序」），右上角出现「完成」按钮，点完成退出；排序模式下才能拖动，不影响正常滚动
- 优化：小题型只能在点前面的 ▸ 图标时展开，避免误触
- 优化：「分析」页改为行测 / 申论两个带下划线的独立标签页

## 1.7.0（2026-09-19）

- 新增：「本次刷什么」改成可展开的二级列表——点前面的 ▸ 才展开小题型，大题型和小题型每行右侧都有 ▶，点一下直接用该模块的默认题量/时长开始计时
- 新增：长按题型行也能展开或收起小题型
- 优化：「分析」页改成行测 / 申论两个独立标签页（带下划线的 Tab，不再是两个小圆片）

## 1.7.0（2026-09-19）

- 新增：「本次刷什么」改成可展开的二级列表——点前面的 ▸ 才展开小题型，大题型与小题型每行右侧都有 ▶，点一下直接用该模块的默认题量/时长开计时
- 新增：长按题型行也能展开/收起小题型
- 保留：长按拖动仍可调整大题型顺序

## 1.6.2（2026-09-19）

- 优化：计时方式进入删除模式后，右上角垃圾桶改为「点一下清空全部预设」（带二次确认）
- 移除：「把 N 分存为预设」这个快捷按钮，预设改为长按管理
- 优化：本次题量只显示数量，不再提供增减按钮（题量跟着所选模块走）

## 1.6.1（2026-09-19）

- 还原：「本次刷什么」只显示大题型，小题型不再堆在列表里（三级结构保留在任务模板的关联模块里）
- 新增：删除操作全部加了二次确认（模块、预设时长、考试节点、任务模板、临时任务）
- 优化：今日任务按行测 / 申论分成两张卡片，各自显示完成进度，不再混排
- 优化：登录页重新设计（渐变欢迎头 + 表单卡片），账号改为 11 位手机号
- 安全：密码要求至少 6 位且同时包含小写字母和数字

## 1.6.0（2026-09-19）

- 新增：行测 / 申论三级题型体系（大题型 → 小题型），刷题模块列表与任务模板的关联模块都按三级结构展示
- 新增：分析页分「行测 / 申论」两个标签，分别查看雷达、估分、趋势与薄弱项
- 优化：「本次刷什么」长按拖动时，被拖的那行跟手移动、放大并浮起，能清楚看到拖动过程
- 优化：小题型的刷题记录自动汇总到所属大题型，估分与雷达口径保持不变
- 修复：旧数据自动迁移到三级结构，刷题与错题记录保留
## 1.5.0（2026-09-19）

- 更新：按 2027 安徽省考最新结构重置行测题型与分值——政治理论（15 题×0.7）、常识判断（15×0.6）、言语理解（25×0.8）、数量关系（15×0.9）、图形推理（5×0.7）、定义判断（10×0.8）、类比推理（10×0.7）、逻辑判断（10×0.9）、资料分析（20×0.9），合计 125 题
- 更新：申论四题分值改为 归纳概括 15 / 综合分析·提出对策 20 / 贯彻执行 25 / 议论文大作文 40
- 修复：旧数据自动迁移到新题型结构（原「判断推理」记录并入图形推理），刷题与错题记录保留
- 修复：「我的 → 关于」版本号现在显示真实版本，不再写死
- 优化：自定义时长输入后直接变成预设，少一步操作
- 优化：「今日任务」「本次刷什么」「每日任务模板」都按行测 / 申论分两块显示
- 优化：改头像改用系统相册选择器（更清晰、能直接看到图）

# 更新记录

每次发版都在这里加一段，格式固定为 `## 版本号（日期）` + 条目列表。
发版脚本会自动把对应版本的条目写进 `version.json`，App 的更新弹窗直接显示这些内容。

## 1.4.0（2026-09-19）

- 优化：「本次刷什么」去掉多余的提示行和上下箭头，改成**长按拖动**调整模块顺序
- 新增：拍照录错题支持**裁剪**（拖动位置、双指缩放，方框内为保留区域）
- 优化：错题列表按**行测 / 申论**分成两组显示，各自标题带数量
- 优化：计时预设时长改为**长按进入删除状态**（右上角出现 🗑，点 ✕ 删掉，点空白处退出），去掉了原来的两个按钮
- 新增：**账号密码登录**（只存本机、不上传服务器），「我的」页顶部显示头像、昵称和签名
- 新增：个人资料编辑，可改昵称、个性签名、头像（拍照或从相册选，支持裁剪）

## 1.3.0（2026-09-19）

- 新增：手机滑动返回可以逐级回退（先关弹窗 → 关子页面 → 回首页 → 再退出应用），计时过程中返回会先让你确认
- 优化：所有输入框换成统一样式（填色底 + 明显边框 + 高亮标签 + "点这里输入"提示），一眼就能看出哪里可以输入
- 优化：打卡日历改成 7 列均匀分布，并加上星期表头
- 优化：勋章墙文字居中、卡片等高，长标题自动换行不再挤成一团
- 优化：计时时长按 ±1 分钟增减；预设时长可以自己添加和删除；「自定义」改成手动输入分钟数
- 优化：「本次刷什么」支持模块的增删改和上下调序（↑ ↓ ✎ ✕）

## 1.2.2（2026-09-19）

- 新增：启动时自动检测更新，发现新版本会弹窗，可选「立即更新」或「稍后更新」（选稍后则下次启动再提醒）
- 新增：更新弹窗展示本次更新内容，装之前先知道改了什么
- 优化：内置更新地址指向 GitHub 最新 Release，以后发新版不用再改设置里的地址

## 1.2.1（2026-09-19）

- 修复：所有带输入框的页面点开就闪退（设置里的起跑日/目标分/提醒时间/更新地址、新增节点、任务模板、手打错题、刷题结算备注）
  - 原因是主题里的字距用了 em 单位，与系统默认的 sp 混用，Material3 输入框做样式插值时直接抛异常
- 加固：新增排版单元测试，锁定「字距必须用 sp」，防止同类问题再次出现

## 1.2.0（2026-09-19）

- 新增：崩溃自记录，闪退时自动把堆栈、机型、系统版本写入本地，可在「我的 → 关于 → 崩溃日志」查看并导出
- 新增：数据写入加固，写文件失败只丢这一次改动，不再把应用带崩（不支持原子写入的设备自动回退到普通写入）
- 更换：应用图标换成「台阶小旗」方案，含 Android 13 单色主题图标

## 1.1.0（2026-09-19）

- 新增：刷题计时（选模块、定题量、正计时/倒计时、屏幕常亮、「做完一题 ＋1」记录每题耗时、标记疑难题、到点通知）
- 新增：刷题结算（录入做了几题、对了几题，自动算正确率与平均每题用时，记录进入水平分析）
- 新增：错题本（拍照 / 选文件 / 直接手打三种录入，复盘页写错因与解题技巧、标记已掌握）
- 新增：知识框架（自建树状结构，掌握度按关联错题自动计算）
- 新增：水平分析（五模块雷达、按安徽省考分值加权估分、正确率趋势、最该补的三块排序）
- 新增：打卡日历、勋章墙、每日一句话（硬核 / 温柔 / 轻松三种语气）
- 新增：桌面小组件、每日计划提醒与考前 7/3/1 天提醒、应用内检查更新

## 1.0.0（2026-09-19）

- 首个可用版本：多节点倒计时（报名开始/截止、笔试、面试，可钉选首页主节点）
- 每日任务模板 + 当天临时任务 + 连续/累计打卡
- 三套主题配色（清新渐变 / 活力撞色 / 贴纸手帐），深浅色跟随系统
- 内置圆体字体（站酷快乐体）
- 考试节点管理、任务模板管理
- 数据只存本机，支持导出 / 导入 JSON 备份
